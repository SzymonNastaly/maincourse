test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

# Cloudflare R2 (S3-compatible), EU jurisdiction. Jurisdiction is fixed at bucket
# creation and cannot be changed later.
# Store keys via: bin/rails credentials:edit
#
# request/response_checksum_* guard against aws-sdk-s3 >= 1.178 CRC32 checksum
# headers. Tested against the live EU bucket 2026-08-26: uploads succeed without
# them, multipart included, so they are insurance against SDK/R2 drift rather
# than a current requirement. Do not blame them for an upload failure untested.
r2:
  service: S3
  endpoint: https://<%= Rails.application.credentials.dig(:r2, :account_id) %>.eu.r2.cloudflarestorage.com
  access_key_id: <%= Rails.application.credentials.dig(:r2, :access_key_id) %>
  secret_access_key: <%= Rails.application.credentials.dig(:r2, :secret_access_key) %>
  region: auto
  bucket: <%= Rails.application.credentials.dig(:r2, :bucket) || "hauptgang-production" %>
  request_checksum_calculation: when_required
  response_checksum_validation: when_required
